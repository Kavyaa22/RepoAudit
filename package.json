{
  "name": "repoaudit",
  "private": true,
  "version": "0.4.0",
  "description": "RepoAudit monorepo — codebase understanding, auditing, and investigation",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "cd backend && python run_dev.py",
    "dev:backend:uvicorn": "cd backend && python -m uvicorn app.main:app --reload --reload-dir app --reload-dir src --host 0.0.0.0 --port 8001",
    "build": "npm run build --prefix frontend",
    "test:backend": "cd backend && python -m pytest",
    "lint:backend": "cd backend && python -m ruff check app"
  },
  "engines": {
    "node": ">=18"
  }
}
